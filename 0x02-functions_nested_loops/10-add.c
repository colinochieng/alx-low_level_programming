#include "main.h"

/**
 * add - prints sum
 *
 * @a: holds space for first number
 * @b: holds space for second number
 *
 * Return: result
 */
int add(int a, int b)
{
	int result = a + b;

	return (result);
}
