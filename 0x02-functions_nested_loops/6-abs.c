#include "main.h"
/**
 *  _abs - computes the absolute value of an integer
 *
 * @int: parameter type
 *  Return: number or 0
 */
int _abs(int number)
{
       	if (number > 0)
		return (number);
	else
		return (-number);
}
