#include <stdio.h>
/**
*main -  prints the alphabet in lowercase,removing q and e
*Return: returns 0
*/
int main(void)
{
int digit = 0;
while (digit < 10)
{
printf("%d\n", digit);
digit++;
}
return (0);
}
